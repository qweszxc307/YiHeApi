<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.crown.projects.mine.mapper.CouponMapper">
    <select id="queryMyCoupon" parameterType="String" resultType="CouponDTO">
  	SELECT  a.id,a.name,a.details,a.type,a.discount,a.discount_point,a.start_time,a.end_time,a.brand_id
	FROM coupon_customer b LEFT JOIN coupon a ON a.id = b.coupon_id   WHERE b.open_id = #{openId}
</select>
	<delete id="deleteCouponCustomerById" parameterType="Integer">
		delete from coupon_customer where id = #{id};
	</delete>
</mapper>
